# WISP Setup

The WISP planner converts a selected scan result into a validated connection plan. It checks interface separation, WISP capability requirements, and administrator-supplied credential references for protected networks.

It does not recover, crack, bypass, or guess Wi-Fi credentials. Runtime application should pass only validated plans to the existing platform adapters and then configure routing/NAT and health monitoring.
